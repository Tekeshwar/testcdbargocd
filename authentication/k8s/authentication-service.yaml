apiVersion: v1
kind: Service
metadata:
  name: authentication-service
  namespace: authentication

spec:
  type: NodePort

  selector:
    app: authentication

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30011